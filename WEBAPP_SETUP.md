# Настройка Web App для сканирования

## Обзор

Web App позволяет открыть камеру прямо в Telegram для сканирования QR-кодов и распознавания серийных номеров.

## Требования

1. **HTTPS сервер** - Web App должен быть размещен на HTTPS сервере (обязательное требование Telegram)
2. **Файл `webapp_scanner.html`** - должен быть доступен по HTTPS URL

## Варианты размещения

### Вариант 1: Использование GitHub Pages (бесплатно)

1. Создайте репозиторий на GitHub
2. Загрузите файл `webapp_scanner.html` в репозиторий
3. Включите GitHub Pages в настройках репозитория
4. URL будет: `https://ваш-username.github.io/репозиторий/webapp_scanner.html`

### Вариант 2: Использование любого веб-хостинга

1. Загрузите файл `webapp_scanner.html` на ваш веб-хостинг
2. Убедитесь, что файл доступен по HTTPS
3. Используйте полный URL файла

### Вариант 3: Локальная разработка с ngrok

Для тестирования локально:

1. Установите ngrok: https://ngrok.com/
2. Запустите локальный веб-сервер:
   ```bash
   python -m http.server 8000
   ```
3. В другом терминале запустите ngrok:
   ```bash
   ngrok http 8000
   ```
4. Используйте HTTPS URL от ngrok (например: `https://abc123.ngrok.io/webapp_scanner.html`)

## Настройка в боте

1. Откройте файл `config.json`
2. Установите `webapp_url` на URL вашего Web App:
   ```json
   {
     "webapp_url": "https://ваш-домен.com/webapp_scanner.html"
   }
   ```

## Функционал Web App

Web App предоставляет два способа сканирования:

1. **Сканирование QR-кода** - использует встроенный сканер Telegram (`showScanQrPopup()`)
2. **Открытие камеры для текста** - открывает камеру устройства для фотографирования серийного номера с последующим OCR распознаванием

## Как это работает

1. Пользователь нажимает "📷 Сканирование" в боте
2. Видит кнопку "📷 Открыть сканер"
3. Нажимает кнопку - открывается Web App
4. Выбирает способ сканирования:
   - QR-код → открывается встроенный сканер Telegram
   - Камера для текста → открывается камера устройства
5. После сканирования/фотографирования данные отправляются обратно в бот
6. Бот обрабатывает данные и находит устройство

## Безопасность

- Web App должен быть размещен на HTTPS
- Telegram проверяет домен перед открытием Web App
- Данные передаются через защищенное соединение

## Устранение неполадок

### Web App не открывается

- Проверьте, что URL начинается с `https://`
- Убедитесь, что файл доступен по указанному URL
- Проверьте настройки в `config.json`

### Камера не открывается

- Убедитесь, что браузер/приложение Telegram имеет разрешение на доступ к камере
- Проверьте настройки приватности устройства

### Данные не приходят в бот

- Проверьте, что обработчик `handle_web_app_data` зарегистрирован в `main.py`
- Убедитесь, что пользователь находится в режиме сканирования

