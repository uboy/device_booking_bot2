# Руководство по устранению неполадок Web App

## Ошибка: `ERR_CONNECTION_RESET`

Эта ошибка означает, что соединение с сервером было неожиданно закрыто. Ниже пошаговая инструкция по диагностике.

## Шаг 1: Проверка доступности сервера

### В браузере (Developer Tools)

1. Откройте Developer Tools (F12 или Ctrl+Shift+I)
2. Перейдите на вкладку **Network** (Сеть)
3. Очистите лог (кнопка 🚫)
4. Попробуйте открыть URL: `https://magic-ball.duckdns.org/webapp_scanner.html`
5. Проверьте запрос в списке:
   - **Статус**: Должен быть 200 (OK) или другой код ошибки
   - **Тип**: `document` или `html`
   - **Время загрузки**: Если очень долго - проблема с сервером

### В терминале (командная строка)

```bash
# Проверка доступности сервера
curl -I https://magic-ball.duckdns.org/webapp_scanner.html

# Или с подробным выводом
curl -v https://magic-ball.duckdns.org/webapp_scanner.html
```

**Ожидаемый результат:**
- `HTTP/2 200` или `HTTP/1.1 200 OK` - сервер доступен
- `Connection refused` - сервер не запущен
- `SSL certificate problem` - проблема с SSL сертификатом
- `Timeout` - сервер не отвечает

## Шаг 2: Проверка SSL сертификата

```bash
# Проверка SSL сертификата
openssl s_client -connect magic-ball.duckdns.org:443 -servername magic-ball.duckdns.org
```

**Что проверить:**
- Сертификат должен быть валидным (не истек)
- Доменное имя должно совпадать
- Цепочка сертификатов должна быть полной

## Шаг 3: Проверка конфигурации веб-сервера

### Если используете Nginx:

Проверьте конфигурацию `/etc/nginx/sites-available/default` или ваш конфиг:

```nginx
server {
    listen 443 ssl http2;
    server_name magic-ball.duckdns.org;
    
    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;
    
    root /var/www/html;  # или путь к вашим файлам
    index webapp_scanner.html;
    
    location / {
        try_files $uri $uri/ =404;
    }
    
    # Важно: правильные заголовки для Web App
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
}
```

**Проверьте:**
- Файл существует по указанному пути
- Права доступа: `chmod 644 webapp_scanner.html`
- Владелец: `chown www-data:www-data webapp_scanner.html` (для Nginx)

### Если используете Apache:

Проверьте конфигурацию виртуального хоста:

```apache
<VirtualHost *:443>
    ServerName magic-ball.duckdns.org
    DocumentRoot /var/www/html
    
    SSLEngine on
    SSLCertificateFile /path/to/cert.pem
    SSLCertificateKeyFile /path/to/key.pem
    
    <Directory /var/www/html>
        Options Indexes FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>
</VirtualHost>
```

## Шаг 4: Проверка логов сервера

### Nginx:
```bash
# Логи ошибок
sudo tail -f /var/log/nginx/error.log

# Логи доступа
sudo tail -f /var/log/nginx/access.log
```

### Apache:
```bash
# Логи ошибок
sudo tail -f /var/log/apache2/error.log

# Логи доступа
sudo tail -f /var/log/apache2/access.log
```

**Что искать:**
- Ошибки 502, 503, 504 - проблемы с бэкендом
- Ошибки SSL/TLS
- Ошибки доступа к файлам (403, 404)
- Connection reset errors

## Шаг 5: Проверка файла webapp_scanner.html

1. **Существует ли файл?**
   ```bash
   ls -la /path/to/webapp_scanner.html
   ```

2. **Правильные ли права доступа?**
   ```bash
   # Должно быть что-то вроде:
   # -rw-r--r-- 1 www-data www-data 12345 webapp_scanner.html
   ```

3. **Правильный ли путь в конфиге?**
   - Проверьте `config.json`: `webapp_url` должен указывать на правильный путь
   - URL должен быть доступен извне (не localhost)

## Шаг 6: Проверка в браузере (детальная диагностика)

### Chrome/Edge Developer Tools:

1. **Network Tab:**
   - Откройте DevTools (F12)
   - Вкладка **Network**
   - Попробуйте загрузить страницу
   - Кликните на запрос `webapp_scanner.html`
   - Проверьте вкладки:
     - **Headers**: Проверьте статус ответа, заголовки
     - **Preview**: Должен показать HTML код
     - **Response**: Полный ответ сервера
     - **Timing**: Время загрузки каждого этапа

2. **Console Tab:**
   - Проверьте ошибки JavaScript
   - Проверьте предупреждения

3. **Security Tab:**
   - Проверьте SSL сертификат
   - Убедитесь, что соединение защищено

### Что проверить в Network:

- **Request URL**: Правильный ли URL?
- **Request Method**: Должен быть `GET`
- **Status Code**: 
  - `200` - OK
  - `404` - файл не найден
  - `403` - нет доступа
  - `500` - ошибка сервера
  - `ERR_CONNECTION_RESET` - соединение сброшено
- **Content-Type**: Должен быть `text/html`
- **Response Headers**: Проверьте наличие заголовков

## Шаг 7: Тестирование с curl

```bash
# Простой запрос
curl https://magic-ball.duckdns.org/webapp_scanner.html

# С сохранением в файл
curl -o test.html https://magic-ball.duckdns.org/webapp_scanner.html

# С проверкой SSL
curl --ssl-no-revoke https://magic-ball.duckdns.org/webapp_scanner.html

# С подробным выводом (самый полезный)
curl -v https://magic-ball.duckdns.org/webapp_scanner.html 2>&1 | tee curl_output.txt
```

**Анализ вывода curl:**
- `* Connected to magic-ball.duckdns.org` - соединение установлено
- `* SSL connection using TLS...` - SSL работает
- `> GET /webapp_scanner.html HTTP/1.1` - запрос отправлен
- `< HTTP/1.1 200 OK` - успешный ответ
- Если видите `* Recv failure: Connection reset by peer` - сервер сбросил соединение

## Шаг 8: Проверка файрвола

```bash
# Проверка открытых портов
sudo netstat -tulpn | grep :443
# или
sudo ss -tulpn | grep :443

# Проверка правил файрвола (ufw)
sudo ufw status

# Проверка правил файрвола (iptables)
sudo iptables -L -n -v
```

**Убедитесь, что:**
- Порт 443 (HTTPS) открыт
- Порт 80 (HTTP) открыт (для редиректа)

## Шаг 9: Проверка DNS

```bash
# Проверка DNS записи
nslookup magic-ball.duckdns.org
# или
dig magic-ball.duckdns.org

# Проверка с разных DNS серверов
dig @8.8.8.8 magic-ball.duckdns.org
dig @1.1.1.1 magic-ball.duckdns.org
```

## Шаг 10: Альтернативные способы размещения

Если проблема с вашим сервером, рассмотрите альтернативы:

### GitHub Pages (бесплатно):
1. Создайте репозиторий на GitHub
2. Загрузите `webapp_scanner.html`
3. Включите GitHub Pages в настройках репозитория
4. URL будет: `https://username.github.io/repository/webapp_scanner.html`

### Netlify (бесплатно):
1. Зарегистрируйтесь на netlify.com
2. Загрузите файл через drag & drop
3. Получите HTTPS URL автоматически

### Vercel (бесплатно):
1. Зарегистрируйтесь на vercel.com
2. Загрузите файл
3. Получите HTTPS URL

## Частые причины ERR_CONNECTION_RESET:

1. **Сервер перегружен** - проверьте нагрузку
2. **Неправильная конфигурация веб-сервера** - проверьте логи
3. **Проблемы с SSL/TLS** - проверьте сертификат
4. **Файрвол блокирует** - проверьте правила
5. **Файл не существует** - проверьте путь
6. **Неправильные права доступа** - проверьте chmod/chown
7. **Проблемы с DuckDNS** - проверьте обновление IP

## Что делать дальше:

1. Выполните все проверки выше
2. Соберите информацию:
   - Вывод `curl -v`
   - Логи веб-сервера
   - Скриншоты из Developer Tools (Network tab)
3. Проанализируйте результаты
4. Исправьте найденные проблемы

## Быстрая проверка:

```bash
# Скрипт для быстрой проверки
#!/bin/bash
URL="https://magic-ball.duckdns.org/webapp_scanner.html"

echo "=== Проверка DNS ==="
nslookup magic-ball.duckdns.org

echo -e "\n=== Проверка доступности ==="
curl -I $URL

echo -e "\n=== Проверка SSL ==="
openssl s_client -connect magic-ball.duckdns.org:443 -servername magic-ball.duckdns.org < /dev/null 2>&1 | grep -A 5 "Certificate chain"

echo -e "\n=== Полный запрос ==="
curl -v $URL 2>&1 | head -30
```

Сохраните как `check_webapp.sh`, сделайте исполняемым (`chmod +x check_webapp.sh`) и запустите.

